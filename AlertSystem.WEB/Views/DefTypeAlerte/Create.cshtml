@model AlertSystem.Entities.Entities.DefTypeAlerte
@{
    ViewData["Title"] = "Créer un type d'alerte";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">
        <i class="bi bi-plus-circle me-2 text-primary"></i>Créer un type d'alerte
    </h1>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            
            <div class="mb-3">
                <label asp-for="TypeAlertId" class="form-label">ID Type Alerte</label>
                <input asp-for="TypeAlertId" class="form-control" type="number" />
                <span asp-validation-for="TypeAlertId" class="text-danger"></span>
                <div class="form-text">Identifiant unique du type d'alerte (nombre entier).</div>
            </div>
            
            <div class="mb-3">
                <label asp-for="AppId" class="form-label">Application</label>
                <select asp-for="AppId" class="form-select" asp-items="ViewBag.Apps">
                    <option value="">-- Sélectionner une application --</option>
                </select>
                <span asp-validation-for="AppId" class="text-danger"></span>
            </div>
            
            <div class="mb-3">
                <label asp-for="Description" class="form-label">Description</label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle me-1"></i>Créer
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="bi bi-x-circle me-1"></i>Annuler
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

